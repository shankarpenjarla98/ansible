- name: install and Run nginx
  hosts: mongodb
  become: yes
  tasks:
  - name: install nginx 
    ansible.builtin.yum:
      name: nginx
      state: installed
  - name: start nginx
    ansible.builtin.yum:
      name: nginx
      state: started